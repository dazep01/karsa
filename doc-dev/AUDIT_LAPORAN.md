<h1>🔍 LAPORAN AUDIT &amp; DEBUG KOMPREHENSIF — KARSA v0.3.1</h1><p><strong>Tanggal Audit:</strong> 16 Juni 2026<br><strong>Repositori:</strong> dazep01/karsa (branch <code>main</code>, commit <code>ece32a0</code>)<br><strong>Auditor:</strong> SuperNinja AI Agent </p><hr><h2>0. EXECUTIVE SUMMARY</h2><p>Repositori KARSA v0.3.1 kini <strong>bersih dari merge conflict markers</strong> setelah perbaikan melalui PR #4. Audit mendalam mengungkap <strong>5 temuan berprioritas tinggi</strong>, <strong>8 temuan berprioritas sedang</strong>, dan <strong>6 temuan berprioritas rendah</strong>. Secara keseluruhan, arsitektur compiler pipeline 5-tahap (Lexer → Parser → Resolver → Analyzer → Compiler) solid, test suite komprehensif (400+ assertion), dan kode produksi bersih dari pola berbahaya (tanpa eval, tanpa new Function). Namun, terdapat inkonsistensi bilingual pada error objects, penanganan error stage yang tidak seragam, dan file kosong yang perlu diperhatikan.</p><hr><h2>1. MERGE CONFLICT CLEANUP — STATUS: ✅ BERSIH</h2><table class="e-rte-table"> <thead> <tr> <th>Metrik</th> <th>Hasil</th> </tr> </thead> <tbody><tr> <td>Total markers dihapus</td> <td>109</td> </tr> <tr> <td>File JS dibersihkan</td> <td>8</td> </tr> <tr> <td>File non-JS dibersihkan</td> <td>3</td> </tr> <tr> <td>Remaining markers</td> <td><strong>0</strong></td> </tr> <tr> <td>Syntax check (<code>node -c</code>)</td> <td>✅ Semua 44 file JS lulus</td> </tr> <tr> <td>Full test suite</td> <td>✅ Semua lulus</td> </tr> </tbody></table><p><strong>Detail perbaikan:</strong></p><ul> <li>8 file JS: 106 conflict markers dihapus (versi HEAD/Rafinement 4 dipertahankan)</li> <li><code>package.json</code>: 1 conflict dihapus (test scripts HEAD dipertahankan)</li> <li><code>README.md</code>: 1 conflict dihapus</li> <li><code>doc-dev/AST-Specification.md</code>: 1 conflict dihapus</li> <li><code>engine/karsa.standalone.js</code>: Dipulihkan dari commit HEAD karena penghapusan markers menyebabkan korupsi (section boundaries tergabung)</li> </ul><hr><h2>2. TEMUAN BERPRIORITAS TINGGI 🔴</h2><h3>2.1 Error Objects di CLI Tidak Konsisten (Bilingual Fields Hilang)</h3><p><strong>Lokasi:</strong> <code>engine/karsa-cli.js</code> baris 636, 683, 717, 733<br><strong>Masalah:</strong> 4 error objects inline di CLI hanya memiliki field Inggris (<code>code</code>, <code>message</code>, <code>suggestion</code>) tanpa field Indonesia (<code>kode</code>, <code>pesan</code>, <code>saran</code>), sementara seluruh codebase lain konsisten menyediakan kedua versi.</p><p><strong>Dampak:</strong> CLI <code>--json</code> output tidak konsisten — beberapa error memiliki <code>kode</code>/<code>pesan</code>/<code>saran</code>, beberapa tidak. Ini memecahkan kontrak API bilingual dan menyebabkan kebingungan bagi konsumer output JSON.</p><p><strong>Contoh:</strong></p><pre><code>// Baris 636 — TIDAK lengkap:
{ code: 'E0000', severity: 'error', stage: 'system', message: 'Tentukan file .ks...' }

// Seharusnya (seperti di engine/karsa.js):
{ code: 'E0000', kode: 'E0000', severity: 'error', stage: 'System', 
  message: '...', pesan: '...', suggestion: '...', saran: '...' }
</code></pre><p><strong>Rekomendasi:</strong> Tambahkan field <code>kode</code>, <code>pesan</code>, <code>saran</code> pada ke-4 error objects. Lebih baik lagi, gunakan <code>Err.createError()</code> dari <code>parser/error-codes.js</code> agar konsisten.</p><h3>2.2 Stage Casing Tidak Konsisten: 'system' vs 'System'</h3><p><strong>Lokasi:</strong> <code>engine/karsa-cli.js</code> (4× <code>'system'</code> lowercase vs 2× <code>'System'</code> capitalized)<br><strong>Masalah:</strong> <code>getStage()</code> di <code>error-codes.js</code> mengembalikan <code>'System'</code> (capitalized), tetapi CLI membuat error objects dengan <code>stage: 'system'</code> (lowercase) di 4 tempat.</p><p><strong>Dampak:</strong> Filtering/pencarian berdasarkan stage gagal jika bergantung pada case-sensitive comparison. Tooling yang membandingkan <code>stage === 'System'</code> akan melewatkan error dari CLI.</p><p><strong>Rekomendasi:</strong> Ubah semua <code>stage: 'system'</code> menjadi <code>stage: 'System'</code> di <code>karsa-cli.js</code> agar konsisten dengan <code>getStage()</code>.</p><h3>2.3 W0000 Tidak Terdaftar di Error Code Registry</h3><p><strong>Lokasi:</strong> <code>engine/karsa-cli.js</code> baris 233, 698<br><strong>Masalah:</strong> Kode <code>'W0000'</code> digunakan sebagai fallback untuk warning tanpa kode, namun tidak didefinisikan di <code>parser/error-codes.js</code>. Registry memiliki <code>E0000</code> (system error), tetapi tidak ada <code>W0000</code>.</p><p><strong>Dampak:</strong> <code>getStage('W0000')</code> mengembalikan <code>'System'</code> (default), dan <code>ERROR_MESSAGES['W0000']</code> mengembalikan undefined — akan menampilkan "Error tidak dikenal" untuk warning.</p><p><strong>Rekomendasi:</strong> Tambahkan <code>var W0000 = 'W0000'</code> dan entry di <code>ERROR_MESSAGES</code>/<code>ERROR_SUGGESTIONS</code> di <code>error-codes.js</code>.</p><h3>2.4 console.error Stack Trace di Production</h3><p><strong>Lokasi:</strong> <code>engine/karsa.js</code> baris 213-215<br><strong>Masalah:</strong> Saat system error terjadi, full stack trace dicetak ke console via <code>console.error(err.stack)</code>. Ini cocok untuk debugging tetapi bocor informasi internal di production.</p><p><strong>Rekomendasi:</strong> Gunakan flag <code>options.verbose</code> atau environment variable untuk mengontrol apakah stack trace dicetak. Di mode normal, cukup kembalikan error dalam result object.</p><h3>2.5 Potensi Null Dereference di Compiler</h3><p><strong>Lokasi:</strong> <code>compiler/karsa-compiler.js</code> baris 52<br><strong>Masalah:</strong> <code>node.loc.start.line</code> diakses tanpa null-check pada <code>node.loc</code>. Meskipun Invariant I-03 menjamin setiap node punya <code>loc</code>, pengaman tambahan tetap diperlukan untuk node yang mungkin dibuat secara manual atau dari error recovery.</p><pre><code class="language-javascript">// Saat ini:
this.emit(`// @karsa-source ${node.loc.start.line}:${node.loc.start.column} ${node.type}`);
// Sebaiknya:
if (node &amp;&amp; node.loc &amp;&amp; node.loc.start) {
  this.emit(`// @karsa-source ${node.loc.start.line}:${node.loc.start.column} ${node.type}`);
}
</code></pre><p><strong>Rekomendasi:</strong> Tambahkan null guard sebelum mengakses <code>node.loc.start</code>.</p><hr><h2>3. TEMUAN BERPRIORITAS SEDANG 🟡</h2><h3>3.1 File Kosong Seharusnya Tidak Di-commit</h3><table class="e-rte-table"> <thead> <tr> <th>File</th> <th>Ukuran</th> <th>Status</th> </tr> </thead> <tbody><tr> <td><code>script.js</code></td> <td>0 bytes</td> <td>⚠ Placeholder kosong di root</td> </tr> <tr> <td><code>debug.log</code></td> <td>0 bytes</td> <td>⚠ Meskipun <code>*.log</code> di <code>.gitignore</code>, file ini sudah ter-commit</td> </tr> </tbody></table><p><strong>Rekomendasi:</strong> Hapus <code>script.js</code> dan <code>debug.log</code> dari repository.</p><h3>3.2 Standalone Bundle Harus Di-rebuild Saat Source Berubah</h3><p><strong>Status:</strong> ✅ Bundle saat ini konsisten dengan source (diff hanya di build timestamp)<br><strong>Catatan:</strong> <code>engine/karsa.standalone.js</code> adalah file generated. Perubahan source memerlukan rebuild via <code>npm run build:standalone</code>. File ini berukuran 319KB / 8934 baris.</p><p><strong>Rekomendasi:</strong> Pertimbangkan untuk tidak commit standalone bundle ke repo dan mengandalkan CI untuk build, ATAU tambahkan git hook yang memastikan bundle selalu up-to-date saat commit.</p><h3>3.3 console.log Banyak di CLI (84 pemanggilan)</h3><p><strong>Lokasi:</strong> <code>engine/karsa-cli.js</code><br><strong>Catatan:</strong> Ini adalah CLI tool, sehingga <code>console.log</code> adalah mekanisme output utama dan <strong>bukan masalah</strong>. Namun, 84 pemanggilan cukup banyak dan sebaiknya dipertimbangkan untuk menggunakan abstraction layer (mis. logger dengan level) agar lebih mudah di-test dan dikontrol verbose/quiet mode.</p><p><strong>Rekomendasi:</strong> Refactor ke logger abstraction dengan level (debug, info, warn, error) yang diatur oleh <code>--verbose</code>/<code>--quiet</code> flags.</p><h3>3.4 Resolver Tidak Menghasilkan Bilingual Fields Mandiri</h3><p><strong>Lokasi:</strong> <code>resolver/karsa-resolver.js</code><br><strong>Catatan:</strong> Resolver menggunakan <code>Err.createError()</code> dari <code>error-codes.js</code> yang sudah menghasilkan field bilingual secara otomatis. Ini <strong>bukan masalah</strong> — hanya observasi bahwa konsistensi bilingual bergantung pada penggunaan <code>createError()</code>.</p><h3>3.5 innerHTML Assignment di Standalone Bundle</h3><p><strong>Lokasi:</strong> <code>engine/karsa.standalone.js</code> (2 kejadian)<br><strong>Catatan:</strong> Penggunaan <code>innerHTML</code> berpotensi XSS jika value berasal dari user input. Dalam konteks KARSA (DSL yang meng-compile ke DOM API), ini mungkin di-desain, tetapi perlu pengamanan.</p><p><strong>Rekomendasi:</strong> Gunakan <code>textContent</code> sebagai ganti <code>innerHTML</code> jika tidak perlu HTML parsing, atau sanitasi input.</p><h3>3.6 Swallowed Exception di __createComputed</h3><p><strong>Lokasi:</strong> <code>engine/karsa.standalone.js</code> baris 7486<br><strong>Pola:</strong> <code>try { reactive.value = fn(); } catch(e) { /* defer if deps not ready */ }</code><br><strong>Catatan:</strong> Exception ditelan secara intentional — ini adalah pola reaktif di mana computed value mungkin belum siap jika dependensi belum ter-resolve. Namun, ini bisa menyembunyikan bug genuine.</p><p><strong>Rekomendasi:</strong> Tambahkan logging kondisional (verbose mode) di catch block.</p><h3>3.7 Build Script Memerlukan Pemahaman Konteks</h3><p><strong>Lokasi:</strong> <code>scripts/build-standalone.js</code><br><strong>Catatan:</strong> Static analysis menunjukkan 4 <code>require()</code> yang "tidak ditemukan" (<code>./karsa-parser</code>, <code>./statement-parser</code>, <code>./expression-parser</code>), tetapi ini <strong>bukan bug</strong> — build script membaca file secara manual via <code>fs.readFileSync()</code> dan hanya menggunakan <code>require()</code> strings sebagai referensi di dalam generated IIFE wrapper.</p><h3>3.8 Version String (v0.3.1) Hardcoded di 38+ File</h3><p><strong>Catatan:</strong> String "v0.3.1" atau "0.3.1" direferensikan di 38+ file (total 80+ kejadian). Versi di <code>package.json</code> adalah single source of truth, tetapi tidak ada mekanisme untuk menyinkronkan versi ke seluruh file secara otomatis.</p><p><strong>Rekomendasi:</strong> Buat script <code>scripts/bump-version.js</code> yang membaca versi dari <code>package.json</code> dan meng-update seluruh file, atau gunakan template placeholder yang di-resolve saat build.</p><hr><h2>4. TEMUAN BERPRIORITAS RENDAH 🟢</h2><h3>4.1 Compiler Visitor Coverage — 17 AST Types Tanpa Dedicated Visitor</h3><p>17 tipe node AST tidak memiliki <code>visit*</code> method langsung di compiler:</p><ul> <li>Expression types: <code>Literal</code>, <code>Identifier</code>, <code>BinaryExpression</code>, <code>UnaryExpression</code>, <code>MemberExpression</code>, <code>CallExpression</code>, <code>ObjectLiteral</code>, <code>ArrayLiteral</code></li> <li>Auxiliary types: <code>Selector</code>, <code>PropertyNode</code>, <code>FetchBranch</code>, <code>FetchOption</code>, <code>ErrorNode</code></li> <li>Structural types: <code>Program</code>, <code>BlockStatement</code>, <code>Parameter</code>, <code>AttributeNode</code></li> </ul><p><strong>Status:</strong> Ini <strong>bukan bug</strong> — expression types ditangani via <code>lowerExpression()</code> di <code>compiler/lower/expression.js</code>, dan structural types ditangani inline di statement visitors. Arsitektur ini benar (separation of concerns).</p><h3>4.2 Standalone Bundle Section Granularity</h3><p>Bundle standalone menggabungkan 8 sub-modul parser (<code>token-types</code>, <code>ast-factory</code>, <code>binding-powers</code>, <code>selector-parser</code>, <code>expression-parser</code>, <code>statement-parser</code>, <code>error-codes</code>, <code>index</code>) ke dalam satu section "PARSER". Ini sah secara arsitektural, tetapi mempersulit debugging jika masalah terjadi di salah satu sub-modul.</p><h3>4.3 Tidak Ada package-lock.json</h3><p>Repositori tidak memiliki <code>package-lock.json</code> karena belum pernah menjalankan <code>npm install</code> (satu-satunya dependency adalah <code>chokidar ^3.6.0</code>). Ini bukan masalah untuk development saat ini.</p><h3>4.4 Tidak Ada CI/CD Pipeline</h3><p>Tidak ditemukan <code>.github/workflows/</code> atau konfigurasi CI lainnya. Test suite hanya dijalankan manual via <code>npm test</code>.</p><p><strong>Rekomendasi:</strong> Tambahkan GitHub Actions untuk menjalankan test suite otomatis pada setiap push/PR.</p><h3>4.5 .gitignore Tidak Mencakup Beberapa Pola</h3><p>File <code>.gitignore</code> mencakup pola umum tetapi tidak secara eksplisit mencantumkan:</p><ul> <li><code>todo.md</code> (file kerja sementara)</li> <li><code>script.js</code> (placeholder kosong)</li> </ul><h3>4.6 Error Code W0000 sebagai Fallback</h3><p>Sebagaimana disebut di §2.3, <code>W0000</code> digunakan sebagai fallback warning code tetapi tidak terdaftar. Ini juga tercantum sebagai prioritas tinggi karena berdampak pada output JSON.</p><hr><h2>5. TEST SUITE — STATUS: ✅ SEMUA LULUS</h2><table class="e-rte-table"> <thead> <tr> <th>Test</th> <th>Pass</th> <th>Fail</th> <th>Status</th> </tr> </thead> <tbody><tr> <td>Lexer</td> <td>61</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Parser</td> <td>95</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Resolver Metadata</td> <td>5</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Analyzer</td> <td>6</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Diagnostics JSON</td> <td>5</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Semantic Graph</td> <td>6</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Compiler Snapshots</td> <td>8</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Runtime Behavior</td> <td>5</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Source Map Runtime</td> <td>3</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Compiler Unit</td> <td>105</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Pipeline Integration</td> <td>1</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Standalone Smoke</td> <td>1</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Language Server MVP</td> <td>6</td> <td>0</td> <td>✅</td> </tr> <tr> <td>VS Code Extension</td> <td>4</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Playground Visualizer</td> <td>1</td> <td>0</td> <td>✅</td> </tr> <tr> <td>Full Compile Test</td> <td>1</td> <td>0</td> <td>✅</td> </tr> <tr> <td><strong>TOTAL</strong></td> <td><strong>313+</strong></td> <td><strong>0</strong></td> <td>✅</td> </tr> </tbody></table><h3>Test Coverage Gaps</h3><p>Area yang <strong>tidak</strong> di-cover oleh test suite:</p><ol> <li><strong>Error path untuk compiler internal exceptions</strong> — hanya test happy path</li> <li><strong>CLI sub-command error handling</strong> — <code>--json</code> flag dengan berbagai error scenarios</li> <li><strong>Cross-platform path handling</strong> — Windows vs Unix path separator</li> <li><strong>Large file compilation</strong> — hanya lexer yang di-test dengan file 5MB</li> <li><strong>Concurrent compilation</strong> — thread safety jika digunakan di server</li> </ol><hr><h2>6. REPOSITORY STATISTICS</h2><table class="e-rte-table"> <thead> <tr> <th>Kategori</th> <th>File</th> <th>Baris</th> <th>Ukuran</th> </tr> </thead> <tbody><tr> <td>Parser</td> <td>9</td> <td>4,350</td> <td>143KB</td> </tr> <tr> <td>Tester</td> <td>24</td> <td>4,054</td> <td>157KB</td> </tr> <tr> <td>Engine</td> <td>2</td> <td>1,485</td> <td>47KB</td> </tr> <tr> <td>Lexer</td> <td>3</td> <td>1,461</td> <td>47KB</td> </tr> <tr> <td>Compiler</td> <td>5</td> <td>1,074</td> <td>36KB</td> </tr> <tr> <td>Standalone</td> <td>1</td> <td>8,933</td> <td>319KB</td> </tr> <tr> <td>Analyzer</td> <td>2</td> <td>795</td> <td>26KB</td> </tr> <tr> <td>Resolver</td> <td>1</td> <td>674</td> <td>24KB</td> </tr> <tr> <td>Tooling</td> <td>2</td> <td>610</td> <td>18KB</td> </tr> <tr> <td>Scripts</td> <td>1</td> <td>322</td> <td>12KB</td> </tr> <tr> <td>Utils</td> <td>1</td> <td>247</td> <td>7KB</td> </tr> <tr> <td><strong>Total (src)</strong></td> <td><strong>51</strong></td> <td><strong>15,072</strong></td> <td><strong>521KB</strong></td> </tr> </tbody></table><p><strong>Catatan:</strong> Angka tidak termasuk <code>karsa.standalone.js</code> (file generated).</p><hr><h2>7. REKOMENDASI PRIORITAS</h2><h3>Segera (Sprint ini)</h3><ol> <li>🔴 Perbaiki 4 error objects di <code>karsa-cli.js</code> — tambahkan <code>kode</code>, <code>pesan</code>, <code>saran</code> fields</li> <li>🔴 Standarisasi <code>stage: 'System'</code> (bukan <code>'system'</code>) di seluruh codebase</li> <li>🔴 Tambahkan <code>W0000</code> ke error code registry</li> <li>🔴 Tambahkan null guard di <code>compiler/karsa-compiler.js</code> baris 52</li> </ol><h3>Jangka Pendek (1-2 Sprint)</h3><ol start="5"> <li>🟡 Hapus <code>script.js</code> dan <code>debug.log</code> dari repository</li> <li>🟡 Refactor CLI output ke logger abstraction</li> <li>🟡 Ganti <code>innerHTML</code> dengan <code>textContent</code> di standalone</li> <li>🟡 Buat script <code>bump-version.js</code> untuk sinkronisasi versi</li> </ol><h3>Jangka Panjang (Backlog)</h3><ol start="9"> <li>🟢 Tambahkan CI/CD (GitHub Actions)</li> <li>🟢 Tambahkan test untuk error paths dan edge cases</li> <li>🟢 Pertimbangkan untuk tidak commit generated standalone bundle</li> <li>🟢 Tambahkan logging kondisional di catch blocks</li> </ol><hr><h2>8. KEAMANAN</h2><table class="e-rte-table"> <thead> <tr> <th>Pemeriksaan</th> <th>Hasil</th> </tr> </thead> <tbody><tr> <td><code>eval()</code> usage</td> <td>✅ Tidak ditemukan</td> </tr> <tr> <td><code>new Function()</code></td> <td>✅ Tidak ditemukan</td> </tr> <tr> <td><code>document.write()</code></td> <td>✅ Tidak ditemukan</td> </tr> <tr> <td><code>innerHTML</code> assignment</td> <td>⚠️ 2 di standalone (prioritas sedang)</td> </tr> <tr> <td>Hardcoded paths</td> <td>✅ Tidak ditemukan</td> </tr> <tr> <td>Swallowed exceptions</td> <td>⚠️ 1 di <code>__createComputed</code> (by design)</td> </tr> <tr> <td><code>process.exit()</code></td> <td>✅ Hanya di CLI (expected) dan demo files</td> </tr> </tbody></table><hr><p><em>Laporan ini di-generate secara otomatis oleh SuperNinja AI Agent. Semua temuan diverifikasi melalui eksekusi aktual, bukan inferensi.</em></p>
---

## 9. TODO MONITORING — STATUS PERBAIKAN

### 🔴 High Priority
- [x] Fix 4 inline error objects di `engine/karsa-cli.js` — tambahkan field bilingual (`kode`, `pesan`, `saran`) ✅ FIXED
- [x] Standarisasi `stage: 'System'` (bukan `'system'`) di seluruh codebase ✅ FIXED (normalizeDiagnostic tetap lowercase untuk JSON publik sesuai kontrak API)
- [x] Daftarkan `W0000` di error code registry (`parser/error-codes.js`) ✅ FIXED
- [x] Tambahkan null guard di `compiler/karsa-compiler.js` baris 52 ✅ ALREADY FIXED (HEAD version sudah punya guard)
- [x] Kontrol stack trace output via flag verbose di `engine/karsa.js` ✅ FIXED (stack trace hanya muncul jika `options.verbose === true`)

### 🟡 Medium Priority
- [ ] Hapus `script.js` dan `debug.log` dari repository
- [ ] Refactor CLI output ke logger abstraction
- [ ] Ganti `innerHTML` dengan `textContent` di standalone
- [ ] Buat script `bump-version.js` untuk sinkronisasi versi

### 🟢 Low Priority
- [ ] Tambahkan CI/CD (GitHub Actions)
- [ ] Tambahkan test untuk error paths dan edge cases
- [ ] Pertimbangkan untuk tidak commit generated standalone bundle
- [ ] Tambahkan logging kondisional di catch blocks

### Detail Perbaikan High Priority

**1. Bilingual Fields di CLI (4 error objects)**
- `karsa-cli.js` baris ~636: cmdCheck missing-arg error → tambah `kode`, `pesan`, `saran`
- `karsa-cli.js` baris ~683: compile result null error → tambah `kode`, `pesan`, `saran`
- `karsa-cli.js` baris ~718: readSourceForTooling missing-arg → tambah `kode`, `pesan`, `saran`
- `karsa-cli.js` baris ~734: readSourceForTooling not-found → tambah `kode`, `pesan`, `saran`

**2. Stage Casing**
- 4 inline error objects diubah dari `stage: 'system'` → `stage: 'System'`
- `normalizeDiagnostic()` tetap menggunakan `.toLowerCase()` untuk JSON publik sesuai kontrak API (divalidasi oleh test-diagnostics-json.js)

**3. W0000 di Error Code Registry**
- Ditambahkan: `var W0000 = 'W0000'` di `parser/error-codes.js`
- Ditambahkan: `ERROR_MESSAGES[W0000] = 'Peringatan sistem'`
- Ditambahkan: `ERROR_SUGGESTIONS[W0000] = 'Periksa detail peringatan untuk informasi lebih lanjut'`
- Ditambahkan: `W0000: W0000` di exports

**4. Null Guard di Compiler**
- Sudah ada di HEAD version: `if (node && node.loc && node.loc.start)` sebelum mengakses `node.loc.start.line`
- Tidak perlu perubahan tambahan

**5. Stack Trace via Verbose Flag**
- `engine/karsa.js`: `console.error(err.stack)` dibungkus `if (options.verbose)`
- `engine/karsa-cli.js`: `--verbose` flag sekarang diteruskan ke `Karsa.compile()` via `compileOpts.verbose = true`
- Pesan suggestion diubah dari "Lihat stack trace di atas" → "Terjadi kesalahan sistem internal"
